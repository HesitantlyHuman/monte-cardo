use crate::ai::game;

fn calculate_state_value(
    game_state: &game::IncompleteInformationGameState,
    num_hypotheticals: usize,
    min_max_steps: usize,
    branch_rollouts: usize,
) -> f32 {
    0.0
}
